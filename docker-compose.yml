version: '2'

volumes:
  resin-data:

services:
  main:
    build:
      context: .
      args:
        GOOS: linux
        GOARCH: arm
        BASE: resin/raspberry-pi2-alpine
    expose:
      - "80"
    network_mode: host
    volumes:
      - 'resin-data:/data'
